<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="freedom.nightq.libraryapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainSampleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".puzzlepicturesample.PuzzlePictureSampleActivity" />
        <activity android:name="freedom.nightq.puzzlepicture.ProcessPicsActivity_" />
        <activity android:name="freedom.nightq.puzzlepicture.ProcessPicsComposeActivity_" />
        <activity android:name=".tagSample.widgets.TagSampleMainActivity" />
        <activity android:name=".tagSample.widgets.AddTagViewActivity" />

    </application>

</manifest>
